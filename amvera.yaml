meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
meta:
  environment: node
  toolchain:
    name: npm
    version: 22
build:
  args:
    - NODE_OPTIONS=--max-old-space-size=1536
run:
  persistence:
    - mountPath: /data
      name: data
  port: 18789
